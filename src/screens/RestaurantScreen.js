import React from 'react';
import { Text, View } from 'react-native';

const RestaurantScreen = () => {
   return (
      <View>
         <Text style={{ fontSize: 19 }}>Restaurant Screen</Text>
      </View>
   );
   // placeholder NEVER INLINE-STYLES
};

export default RestaurantScreen;
